# Team Lead Learnings — 2026-04-23 declaw branch

## Codebase Patterns
- discovery.ts uses `as WorkspaceConfig` for daemon API response — should parse through schema but internal call makes it low-risk
- Ferox defined DynamicProviderInput types locally in translator.ts to avoid cross-package dep on apps/link — good pattern for registry→link boundary
- Both deno.json AND package.json exports must be updated for new subpaths — vitest uses Node resolution (package.json), Deno uses deno.json

## Testing
- discovery.test.ts has two `beforeEach` blocks where the second should be `afterEach` (restoring process.env) — works but confusing
- Test fixture builders commonly need `as Type` for spread patterns that lose type precision — not worth fighting
- `as unknown as AtlasUIMessage["parts"][number]` casts needed for test message fixtures with strict AI SDK union types — no good alternative
- vi.hoisted() required when mock factories reference mutable variables tests need to mutate between runs

## Process
- Teammate scope creep: Almond (#102) refactored fetchAllTools into discoverMCPServers — that was Ponderosa's task (#105). Caused a bug (removed stringifyError import used elsewhere). Assign overlapping files sequentially next time.
- Jinju (#103) missed 2 AC items (provider/requiredConfig fields). Always cross-check AC items against the actual implementation, not just the test count.
- delegate/index.ts uses `import()` in type position for MCPServerCandidate — should be a static import at the top alongside the existing discovery module import
- `serverEntries[i]!` non-null assertions in bounded for-loops — pragmatic trade-off under noUncheckedIndexedAccess, noting for CLAUDE.md
