version: 2
sql:
  - engine: postgresql
    queries: query.sql
    schema:
      - schema.sql
    gen:
      go:
        package: litellmrepo
        out: .
        sql_package: pgx/v5
        omit_unused_structs: true
