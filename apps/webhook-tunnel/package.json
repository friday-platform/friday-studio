{
  "name": "@atlas/webhook-tunnel",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "description": "Webhook tunnel service — receives external webhooks via Cloudflare tunnel and forwards to atlasd",
  "dependencies": {
    "@atlas/logger": "workspace:*",
    "cloudflared": "^0.7.1",
    "hono": "4.12.12",
    "@std/yaml": "npm:@jsr/std__yaml@^1.0.10",
    "random-words": "^2.0.1",
    "zod": "^4.3.5"
  }
}
