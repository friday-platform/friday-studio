name: "Upload to Google Cloud Storage (Sandbox)"
description: "Uploads artifacts to sandbox GCS bucket"

inputs:
  bucket-prefix:
    description: "Prefix for the bucket path (e.g., edge/, nightly/, or empty for release)"
    required: false
    default: ""
  version:
    description: "Version string for the artifacts"
    required: true

runs:
  using: "composite"
  steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts/
        merge-multiple: true

    - name: Authenticate to Google Cloud (Sandbox)
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: projects/415092695556/locations/global/workloadIdentityPools/github/providers/atlas-github
        service_account: atlas-github-ci@tempest-sandbox.iam.gserviceaccount.com
        create_credentials_file: true
        export_environment_variables: true

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Upload artifacts to sandbox GCS
      shell: bash
      run: |
        VERSION="${{ inputs.version }}"
        cd ./artifacts

        # Upload all artifacts to sandbox bucket with version prefix (flat structure)
        if ls atlas_*.tar.gz 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.tar.gz gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi
        if ls atlas_*.zip 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.zip gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi
        if ls atlas_*.pkg 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.pkg gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi
        if ls atlas_*.msi 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.msi gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi
        # Upload Electron installers (new naming convention)
        if ls atlas_*.exe 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.exe gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi
        if ls atlas_*.AppImage 1> /dev/null 2>&1; then
          gsutil -m cp atlas_*.AppImage gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}
        fi

        echo "Uploaded ${{ inputs.bucket-prefix }} artifacts to sandbox GCS bucket"

    - name: Cleanup old artifacts (sandbox)
      shell: bash
      run: |
        cd ./artifacts

        # Only cleanup for edge and nightly channels to prevent removing stable releases
        if [ "${{ inputs.bucket-prefix }}" == "edge/" ] || [ "${{ inputs.bucket-prefix }}" == "nightly/" ]; then
          echo "Cleaning up old ${{ inputs.bucket-prefix }} versions in sandbox (keeping last 7)..."

          # Extract channel prefix for file matching (e.g., "edge" or "nightly")
          CHANNEL=$(echo "${{ inputs.bucket-prefix }}" | sed 's/\///g')

          # Cleanup artifacts: Keep last 7 versions (28 files = 4 platforms × 7 versions)
          gsutil ls gs://tempest-sandbox-atlas-binaries/${{ inputs.bucket-prefix }}atlas_${CHANNEL}-*.tar.gz | \
            sort -r | \
            tail -n +29 | \
            head -20 | \
            xargs -r gsutil -m rm || echo "No old ${CHANNEL} artifacts to clean"


          echo "Sandbox ${{ inputs.bucket-prefix }} cleanup completed"
        else
          echo "Skipping cleanup for release channel (stable releases preserved)"
        fi
