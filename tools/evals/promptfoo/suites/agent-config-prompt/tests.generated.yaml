# AUTO-GENERATED by render.ts. Do NOT edit by hand.
# Source: render.ts (which calls the real composeAgentPrompt).
# Regenerate with: deno task evals:render-promptfoo

- description: marker-token — config prompt instruction (marker token) is honored alongside action prompt
  vars:
    user_prompt: >-
      You are a helpful assistant. Always end every response with the literal token "NEON_GREEN_OK"
      on its own line. This is a non-negotiable formatting rule.


      Introduce yourself in one short sentence.


      ## Context Facts

      - Current Date: 2026-05-06
  assert:
    - type: contains
      value: NEON_GREEN_OK
      metric: HonorsConfigPrompt
    - type: icontains-all
      value:
        - assistant
      metric: AddressesActionPrompt
- description: >-
    interpolated-action-with-config-rule — config prompt is honored when action prompt uses
    {{inputs.x}} interpolation
  vars:
    user_prompt: >-
      You answer questions about visual design. Always include the literal token "BG_NEON_GREEN"
      verbatim somewhere in your response.


      Describe a robot chef sprite in two sentences.


      ## Context Facts

      - Current Date: 2026-05-06
  assert:
    - type: contains
      value: BG_NEON_GREEN
      metric: HonorsConfigPrompt
    - type: icontains-all
      value:
        - robot
        - chef
      metric: AddressesActionPrompt
- description: config-prompt-alone — config prompt drives behavior when no action prompt is set
  vars:
    user_prompt: >-
      Reply with exactly one short sentence introducing yourself, and append the literal token
      "SOLO_OK" at the end.


      ## Context Facts

      - Current Date: 2026-05-06
  assert:
    - type: contains
      value: SOLO_OK
      metric: HonorsConfigPrompt
