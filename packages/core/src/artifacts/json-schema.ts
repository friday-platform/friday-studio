/**
 * Re-exported from @atlas/schemas/json-schema.
 * Kept for backward compatibility — all consumers can continue importing from
 * "@atlas/core/artifacts".
 */
export {
  JSONSchemaSchema,
  type ValidatedJSONSchema,
} from "@atlas/schemas/json-schema";
